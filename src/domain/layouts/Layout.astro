---
import ToggleTheme from "@/domain/components/ToogleTheme";
import Navigation from "./components/Navigation";
import "@/domain/styles/global.css";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

const user = Astro.locals.auth.user;

const queryClient = new QueryClient();
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>Messaging</title>
  </head>
  <body class="bg-background dark:bg-background">
    <div
      class="w-screen max-w-full flex gap-1 items-center justify-end p-4 shadow bg-zinc-100 dark:bg-zinc-900"
    >
      <h2 class="mr-auto font-extrabold">Prat🍛o</h2>
      <Navigation client:only user={user} />
      <ToggleTheme client:only />
    </div>
    <div class="p-4">
      <slot />
    </div>
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
